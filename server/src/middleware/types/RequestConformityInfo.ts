// NOTE: Can expand this depending on various strategies of Rate Limiter (e.g. ip address, geolocation, timestamps...)
type RequestConformityInfo = {
	userId: string
};

export type { RequestConformityInfo };